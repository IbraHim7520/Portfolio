/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // makes the site fully static
  // optional: if you want trailing slashes for pages
  // trailingSlash: true,
};

module.exports = nextConfig;
